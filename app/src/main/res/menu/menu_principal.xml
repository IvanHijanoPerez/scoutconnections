<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/accion_logout"
        android:title="Cerrar sesión"
        android:fontFamily="@font/nunito_sans_extrabold" />

    <item android:id="@+id/accion_buscar"
        android:title="Buscar"
        android:fontFamily="@font/nunito_sans_extrabold"
        android:icon="@drawable/ic_buscar"
        app:showAsAction="always"
        app:actionViewClass="android.widget.SearchView"/>

    <item android:id="@+id/accion_usuarios"
        android:title="Usuarios"
        android:fontFamily="@font/nunito_sans_extrabold"
        android:icon="@drawable/ic_people_24"
        app:showAsAction="always"/>

    <item android:id="@+id/accion_anadir_post"
        android:title="Añadir post"
        android:fontFamily="@font/nunito_sans_extrabold"
        android:icon="@drawable/ic_add_post"
        app:showAsAction="always"/>

</menu>